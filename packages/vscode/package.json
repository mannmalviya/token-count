{
  "name": "token-count-vscode",
  "displayName": "Token Count",
  "description": "Live Claude Code token usage in the status bar + a dashboard.",
  "version": "0.0.0",
  "private": true,
  "publisher": "token-count-local",
  "engines": {
    "vscode": "^1.85.0"
  },
  "main": "./dist/extension.cjs",
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {
    "commands": [
      {
        "command": "tokenCount.showDashboard",
        "title": "Token Count: Show Dashboard"
      }
    ]
  },
  "categories": ["Other"],
  "scripts": {
    "build": "node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --watch",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@token-count/core": "workspace:*"
  },
  "devDependencies": {
    "@types/vscode": "^1.85.0",
    "esbuild": "^0.24.0"
  }
}
